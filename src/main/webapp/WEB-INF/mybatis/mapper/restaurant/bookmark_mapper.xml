<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
		   "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		   
<mapper namespace="bookmark_mapper">

	<select id="findByUserIdAndRestaurantId" resultType="Bookmark">
        SELECT * FROM mp_bookmark 
        WHERE user_id = #{userId} AND restaurant_id = #{restaurantId}
    </select>
    
    <insert id="insert">
        INSERT INTO mp_bookmark ( ID, USER_ID, RESTAURANT_ID, STATUS, CREATED_AT)
        VALUES (mp_bookmark_seq.NEXTVAL ,#{userId}, #{restaurantId}, #{status}, SYSTIMESTAMP)
    </insert>
    
    <update id="updateStatus">
        UPDATE mp_bookmark 
        SET 
            STATUS = #{status}, 
            UPDATED_AT = SYSTIMESTAMP
        WHERE ID = #{id}
    </update>
    
    <select id="getBookmarksbyUserId" resultType="Bookmark">
    	SELECT * FROM mp_bookmark
    	WHERE user_Id = #{userId} AND status = 'true'
    </select>

</mapper>
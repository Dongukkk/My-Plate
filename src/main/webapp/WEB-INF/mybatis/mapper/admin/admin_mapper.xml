<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
		   "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin_mapper">

	<!-- 식당 관리 -->
	<select id="findRestaurantList" resultType="AdminRestaurant">
		SELECT
		R.ID AS id,
		R.NAME AS name,
		R.CATEGORY AS category,
		R.ADDRESS AS address,
		NVL(R.SOLO_INDEX, 0) AS solo_index,
		NVL(R.AVG_RATING, 0) AS avgRating,
		NVL(R.RATING_COUNT, 0) AS rating_count,
		R.STATUS AS status,
		TO_CHAR(R.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(R.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM
		MP_RESTAURANT R
		WHERE R.DELETED_AT IS NULL
		ORDER BY R.ID
	</select>

	<select id="findRestaurantById" parameterType="long"
		resultType="AdminRestaurant">
		SELECT
		ID AS id,
		NAME AS name,
		DESCRIPTION AS description,
		CATEGORY AS category,
		AVG_RATING AS avgRating,
		RATING_COUNT AS
		rating_count,
		SOLO_INDEX AS solo_index,
		PHOTO_URL AS photo_url,
		ADDRESS
		AS address,
		PHONE AS phone,
		STATUS AS status,
		TO_CHAR(created_at,
		'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(updated_at,
		'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_RESTAURANT
		WHERE ID =
		#{id} AND DELETED_AT IS NULL
	</select>

	<update id="modifyAdminRestaurant"
		parameterType="AdminRestaurant">
		UPDATE MP_RESTAURANT
		SET NAME = #{name},
		DESCRIPTION =
		#{description},
		CATEGORY = #{category},
		PHOTO_URL = #{photo_url},
		ADDRESS = #{address},
		PHONE = #{phone},
		STATUS = #{status},
		UPDATED_AT =
		SYSTIMESTAMP
		WHERE ID = #{id}
	</update>

	<insert id="saveAdminRestaurant" parameterType="AdminRestaurant">
		<selectKey keyProperty="id" resultType="long" order="BEFORE">SELECT
			MP_RESTAURANT_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO MP_RESTAURANT (ID, NAME, DESCRIPTION, CATEGORY, PHOTO_URL,
		ADDRESS, PHONE, STATUS, CREATED_AT, UPDATED_AT) VALUES (
		MP_RESTAURANT_SEQ.NEXTVAL,
		#{name},
		#{description},
		#{category},
		#{photo_url},
		#{address},
		#{phone},
		#{status},
		SYSTIMESTAMP,
		SYSTIMESTAMP
		)
	</insert>

	<update id="DeleteAdminRestaurant"
		parameterType="AdminRestaurant">
		UPDATE MP_RESTAURANT
		SET STATUS = 'DELETED',
		DELETED_AT =
		SYSTIMESTAMP
		WHERE ID = #{id}
	</update>


	<!-- 사용자 관리 -->
	<select id="findUserList" resultType="AdminUser">
		SELECT
		U.ID AS id,
		U.EMAIL AS
		email,
		U.USERNAME AS username,
		U.ADDRESS AS address,
		U.PHONE_NUMBER AS
		phoneNumber,
		U.ROLE AS role,
		U.STATUS AS status,
		TO_CHAR(U.CREATED_AT,
		'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(U.UPDATED_AT,
		'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_USER U
		WHERE U.ROLE !=
		'ADM'
		ORDER BY U.ID
	</select>

	<update id="DeleteAdminUser" parameterType="AdminUser">
		UPDATE MP_USER
		SET
		STATUS = 'DELETED',
		leave_date = SYSTIMESTAMP
		WHERE ID = #{id}
	</update>

	<select id="findUserById" parameterType="long"
		resultType="AdminUser">
		SELECT
		U.ID AS id,
		U.EMAIL AS email,
		U.USERNAME AS username,
		U.ADDRESS AS address,
		U.PHONE_NUMBER AS phoneNumber,
		U.ROLE AS role,
		U.STATUS AS status,
		TO_CHAR(U.CREATED_AT, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(U.UPDATED_AT, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_USER U
		WHERE U.ID = #{id} AND U.LEAVE_DATE IS NULL AND U.ROLE != 'ADM'
	</select>

	<update id="modifyAdminUser" parameterType="AdminUser">
		UPDATE MP_USER
		SET
		USERNAME = #{username},
		ADDRESS = #{address},
		PHONE_NUMBER =
		#{phoneNumber},
		STATUS = #{status},
		UPDATED_AT = SYSTIMESTAMP
		WHERE ID =
		#{id} AND LEAVE_DATE IS NULL
	</update>

	<!-- ================= 신고 관리 (UR/OTH/RER/IPC) ================= -->
	<!-- 목록 -->
	<select id="findUserReportList" resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'UR'
		ORDER BY r.created_at DESC
	</select>

	<select id="findOTHReportList" resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'OTH'
		ORDER BY r.created_at DESC
	</select>

	<select id="findRERReportList" resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'RER'
		ORDER BY r.created_at DESC
	</select>

	<select id="findIPCReportList" resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'IPC'
		ORDER BY r.created_at DESC
	</select>

	<!-- 상세 (id 조건 포함) -->
	<select id="searchURReportsById" parameterType="long"
		resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'UR' AND r.id = #{id}
	</select>

	<select id="searchOTHReportsById" parameterType="long"
		resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'OTH' AND r.id = #{id}
	</select>

	<select id="searchRERReportsById" parameterType="long"
		resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'RER' AND r.id = #{id}
	</select>

	<select id="searchIPCReportsById" parameterType="long"	resultType="AdminReport">
		SELECT
		r.id AS id,
		r.reporter_id AS reporterId,
		r.reported_item_type AS reportedItemType,
		r.reason AS reason,
		r.status AS status,
		r.decision AS decision,
		r.memo AS memo,
		r.excerpt AS excerpt,
		TO_CHAR(r.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
		TO_CHAR(r.updated_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS updatedAt
		FROM MP_REPORT r
		WHERE r.reported_item_type = 'IPC' AND r.id = #{id}
	</select>

	<!-- 업데이트 -->
	<update id="updateURReport" parameterType="map">
		UPDATE MP_REPORT
		SET
		status = #{status, jdbcType=VARCHAR},
		decision = #{decision, jdbcType=VARCHAR},
		memo = #{memo, jdbcType=VARCHAR},
		excerpt = #{excerpt, jdbcType=VARCHAR},
		updated_at = SYSTIMESTAMP
		WHERE id = #{id} AND reported_item_type = 'UR'
	</update>

	<update id="updateOTHReport" parameterType="map">
		UPDATE MP_REPORT
		SET
		status = #{status, jdbcType=VARCHAR},
		decision = #{decision, jdbcType=VARCHAR},
		memo = #{memo, jdbcType=VARCHAR},
		excerpt = #{excerpt, jdbcType=VARCHAR},
		updated_at = SYSTIMESTAMP
		WHERE id = #{id} AND reported_item_type = 'OTH'
	</update>

	<update id="updateRERReport" parameterType="map">
		UPDATE MP_REPORT
		SET
		status = #{status, jdbcType=VARCHAR},
		decision = #{decision, jdbcType=VARCHAR},
		memo = #{memo, jdbcType=VARCHAR},
		excerpt = #{excerpt, jdbcType=VARCHAR},
		updated_at = SYSTIMESTAMP
		WHERE id = #{id} AND reported_item_type = 'RER'
	</update>

	<update id="updateIPCReport" parameterType="map">
		UPDATE MP_REPORT
		SET
		status = #{status, jdbcType=VARCHAR},
		decision = #{decision, jdbcType=VARCHAR},
		memo = #{memo, jdbcType=VARCHAR},
		excerpt = #{excerpt, jdbcType=VARCHAR},
		updated_at = SYSTIMESTAMP
		WHERE id = #{id} AND reported_item_type = 'IPC'
	</update>



	<!-- ================= 최근 처리 이력 ================= -->
	<!-- 사용자 신고 (UR) -->
	<select id="findRecentActionsUR" resultType="AdminAction">
	    SELECT
	        a.id,
	        a.report_id     AS reportId,
	        a.item_type     AS itemType,
	        a.reporter_id   AS reporterId,
	        a.action        AS action,
	        a.status_after  AS statusAfter,
	        a.memo,
	        a.excerpt,
	        TO_CHAR(a.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt
	    FROM MP_REPORT_ACTION a
	    WHERE a.item_type = 'UR'
	    ORDER BY a.created_at DESC
	</select>
	
	<!-- 기타 문의 (OTH) -->
	<select id="findRecentActionsOTH" resultType="AdminAction">
	    SELECT
	        a.id,
	        a.report_id     AS reportId,
	        a.item_type     AS itemType,
	        a.reporter_id   AS reporterId,
	        a.action        AS action,
	        a.status_after  AS statusAfter,
	        a.memo,
	        a.excerpt,
	        TO_CHAR(a.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
	        r.reason        AS reason
	    FROM MP_REPORT_ACTION a
	    LEFT JOIN MP_REPORT r ON r.id = a.report_id
	    WHERE a.item_type = 'OTH'
	    ORDER BY a.created_at DESC
	</select>
	
	<!-- 가게 정보 수정 요청 (RER) -->
	<select id="findRecentActionsRER" resultType="AdminAction">
	    SELECT
	        a.id,
	        a.report_id     AS reportId,
	        a.item_type     AS itemType,
	        a.reporter_id   AS reporterId,
	        a.action        AS action,
	        a.status_after  AS statusAfter,
	        a.memo,
	        a.excerpt,
	        TO_CHAR(a.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
	        r.reason        AS reason
	    FROM MP_REPORT_ACTION a
	    LEFT JOIN MP_REPORT r ON r.id = a.report_id
	    WHERE a.item_type = 'RER'
	    ORDER BY a.created_at DESC
	</select>
	
	<!-- 부적절 콘텐츠 (IPC) -->
	<select id="findRecentActionsIPC" resultType="AdminAction">
	    SELECT
	        a.id,
	        a.report_id     AS reportId,
	        a.item_type     AS itemType,
	        a.reporter_id   AS reporterId,
	        a.action        AS action,
	        a.status_after  AS statusAfter,
	        a.memo,
	        a.excerpt,
	        TO_CHAR(a.created_at, 'YYYY-MM-DD"T"HH24:MI:SS') AS createdAt,
	        r.reason        AS reason
	    FROM MP_REPORT_ACTION a
	    LEFT JOIN MP_REPORT r ON r.id = a.report_id
	    WHERE a.item_type = 'IPC'
	    ORDER BY a.created_at DESC
	</select>
	
	<update id="updateReport" parameterType="AdminReportDTO">
	    UPDATE MP_REPORT
	    SET
	        status = #{status, jdbcType=VARCHAR},
	        decision = #{decision, jdbcType=VARCHAR},
	        memo = #{memo, jdbcType=VARCHAR},
	        excerpt = #{excerpt, jdbcType=VARCHAR},
	        updated_at = SYSTIMESTAMP
	    WHERE id = #{id}
	      AND reported_item_type = #{reportedItemType}
	</update>
	
	<insert id="insertReportAction" parameterType="AdminReportDTO">
	    INSERT INTO MP_REPORT_ACTION (
	        id, report_id, item_type, reporter_id,
	        action, status_after, memo, excerpt, created_at
	    ) VALUES (
	        MP_REPORT_ACTION_SEQ.NEXTVAL,
	        #{id},
	        #{reportedItemType, jdbcType=VARCHAR}, 
	        #{reporterId, jdbcType=NUMERIC},
	        NVL(TRIM(#{decision, jdbcType=VARCHAR}), 'NONE'),
    		NVL(TRIM(#{status, jdbcType=VARCHAR}),   'PENDING'),
	        #{memo, jdbcType=VARCHAR},
	        #{excerpt, jdbcType=VARCHAR},
	        SYSTIMESTAMP
	    )
	</insert>

  <!-- 로그인 -->
  <select id="findAdminForLoginByEmail" parameterType="string" resultType="AdminUser">
    SELECT
      U.ID        AS id,
      U.EMAIL     AS email,
      U.PASSWORD  AS password,
      U.USERNAME  AS username,
      U.ROLE      AS role,
      U.STATUS    AS status
    FROM MP_USER U
    WHERE U.EMAIL = #{email}
      AND U.ROLE = 'ADM'
      AND U.STATUS = 'ACTIVE'
  </select>

  <insert id="insertIPCReport" parameterType="AdminReport">
    <selectKey keyProperty="id" resultType="long" order="BEFORE"> SELECT MP_REPORT_SEQ.NEXTVAL FROM DUAL </selectKey>
	INSERT INTO MP_REPORT (
	ID, REPORTER_ID, REPORTED_ITEM_TYPE, REPORTED_ITEM_ID, REASON, MEMO,
	EXCERPT, STATUS, CREATED_AT, UPDATED_AT) 
	VALUES ( #{id}, #{reporterId}, 'IPC', #{reportedItemId}, #{reason}, #{memo},
	NVL(#{excerpt}, ''), NVL(#{status}, 'PENDING'), SYSTIMESTAMP, SYSTIMESTAMP)
  </insert>


</mapper>